# 원본 코드 vs 우리 코드: 한 줄씩 완전 대조

> 비교 대상:
> - **원본**: `rabbit-abacus/roleofimagebackgrounds/main.py` (~422줄, val only, single synset, GrabCut)
> - **우리**: `in9-dataset-generator/generate_in9.py` (~977줄, train/val, 9 SC 전체, SAM 3)

---

## 0. 요약: 원본과 다른 모든 것

총 **18개 차이점** 발견. 이 중:
- **버그(수정완료)**: 1건
- **의도적 변경 (기능 확장)**: 6건
- **의도적 변경 (품질 개선)**: 3건
- **임의 판단 (defensive coding)**: 5건
- **임의 판단 (코드 스타일)**: 3건

---

## 1. Transforms (원본 line 10-16 / 우리 line 77-83)

| 항목 | 원본 | 우리 | 동일 |
|------|------|------|:---:|
| standard_transform | `Resize(256, NEAREST) + CenterCrop(256)` | 동일 | O |
| test_transform | `Resize(256, NEAREST) + CenterCrop(224)` | 동일 | O |

**차이 없음.**

---

## 2. blackout() (원본 line 28-34 / 우리 line 268-278)

| 항목 | 원본 | 우리 | 동일 |
|------|------|------|:---:|
| 로직 | `np_img[np_mask==255] = 0` | 동일 | O |
| grayscale 처리 | `np.stack((np_img,)*3, axis=-1)` | 동일 | O |

**차이 없음.**

---

## 3. combine() (원본 line 39-44 / 우리 line 281-291)

| 항목 | 원본 | 우리 | 동일 |
|------|------|------|:---:|
| 로직 | `np_img1[np_mask==255] = np_img2[np_mask==255]` | 동일 | O |

**차이 없음.**

---

## 4. get_fg_mask() → GrabCutSegmentor.segment() (원본 line 50-71 / 우리 line 100-137)

| 항목 | 원본 | 우리 | 동일 |
|------|------|------|:---:|
| rect 계산 | `(xmin, ymin, xmax-xmin, ymax-ymin)` | 동일 (line 122) | O |
| GrabCut 호출 | `cv2.grabCut(..., 5, cv2.GC_INIT_WITH_RECT)` | 동일 (line 124-125) | O |
| mask 해석 | `np.where((mask==2)\|(mask==0), 0, 1)` | 동일 (line 130) | O |
| MIN_MASK_AMOUNT | `np.sum(fg_mask) / bbox_area < 0.1` | 동일 (line 135-136) | O |
| bgmodel/fgmodel | `np.zeros((1,65), np.float64)` | 동일 (line 119-120) | O |
| **예외 처리** | `except:` (bare except) | `except Exception:` | **차이 #1** |
| **bbox_area==0** | 체크 없음 | `if bbox_area == 0: return None, False` | **차이 #2** |

### 차이 #1: bare except vs except Exception [임의 판단 — 코드 스타일]

**원본** (`main.py:56-59`):
```python
try:
    cv2.grabCut(np_img, mask, rect, bgmodel, fgmodel, 5, cv2.GC_INIT_WITH_RECT)
except:
    return None, False
```

**우리** (`generate_in9.py:125-129`):
```python
try:
    cv2.grabCut(img_np, mask, rect, bgmodel, fgmodel,
                GRABCUT_ITER, cv2.GC_INIT_WITH_RECT)
except Exception:
    return None, False
```

- **왜**: bare `except:`는 `KeyboardInterrupt`, `SystemExit`도 삼킨다. Ctrl+C로 중단하려 해도
  무시되는 문제. Python 커뮤니티에서 bare except는 안티패턴으로 간주.
  실질적으로 GrabCut이 이 예외를 발생시키지 않으므로 **결과 차이 없음**.

### 차이 #2: bbox_area == 0 체크 [임의 판단 — defensive coding]

**원본** (`main.py:64`):
```python
mask_proportion = np.sum(fg_mask) / ((xmax - xmin) * (ymax - ymin))
# → bbox_area가 0이면 ZeroDivisionError → 프로그램 크래시
```

**우리** (`generate_in9.py:134-137`):
```python
bbox_area = (xmax - xmin) * (ymax - ymin)
if bbox_area == 0:
    return None, False
mask_proportion = np.sum(fg_mask) / bbox_area
```

- **왜**: xmin==xmax 또는 ymin==ymax인 degenerate bbox가 있으면 원본은 ZeroDivisionError.
  실제 ImageNet에 이런 케이스는 없으므로 **결과 차이 없음**이지만, 방어적으로 추가.

---

## 5. get_bg_tiled() (원본 line 76-107 / 우리 line 294-330)

| 항목 | 원본 | 우리 | 동일 |
|------|------|------|:---:|
| margin 계산 | 동일 | 동일 | O |
| tile 선택 로직 | 동일 | 동일 | O |
| tile 반복 로직 | 동일 | 동일 | O |
| **assert vs if** | `assert max(...) > 0` → ValueError raise | `if max(...) <= 0: raise ValueError` | O (동일 동작) |

**차이 없음.** (assert를 if로 바꿨으나 동일하게 ValueError를 raise)

---

## 6. is_good_image() (원본 line 121-186 / 우리 line 405-446)

### 시그니처 차이

| | 원본 | 우리 |
|---|---|---|
| 인자 | `(in_dir, ann_dir, synset, image_num)` | `(img_pil, ann_width, ann_height, bboxes, split)` |
| 파일 I/O | 함수 내부에서 이미지/XML 로드 | 호출자가 미리 로드하여 전달 |
| return | 0~6 | 0, 3, 4, 5, 6 (1=파일없음, 2=annotation없음은 호출자에서 처리) |

시그니처 변경은 **기능 확장** (val 하드코딩 제거, train 지원)을 위한 것이며, 내부 로직은 동일.

### 필터링 로직 한 줄씩 비교

| 체크 | 원본 코드 (line) | 우리 코드 (line) | 동일 |
|------|-----------------|-----------------|:---:|
| 이미지 존재 | `os.path.exists(image_path)` (126) | 호출자에서 처리 | 동일 의미 |
| annotation 존재 | `os.path.exists(annotation_path)` (130) | 호출자에서 처리 | 동일 의미 |
| dimension 일치 | `width != annotated_width` (145) | `ann_width != width` (418) | O |
| single bbox | `len(all_bounding_boxes) != 1` (150) | `len(bboxes) != 1` (421) | O |
| bbox mask 생성 | `Image.new("RGB", (w,h), (0,0,0))` + `draw.rectangle(fill="white")` (163-165) | 동일 (427-429) | O |
| **FRACTION check** | `post_crop = test_transform(mask)` (167) | `post_crop = test_transform(mask)` (434) | **O (수정 후)** |
| num_masked_pixels | `np.sum(post_crop) // (255*3)` (168) | 동일 (435) | O |
| area_ratio | `num_masked_pixels / 224**2` (170) | `num_masked_pixels / (TEST_CROP**2)` (436) | O |
| FRACTION threshold | `> 0.9` (171) | 동일 (437) | O |
| FRACTION_2 check | `Resize(256, NEAREST)(mask)` (178) | 동일 (441) | O |
| FRACTION_2 비교 | `num_masked_pixels < 0.5 * num_masked_pixels_resize` (180) | 동일 (443) | O |

### ~~버그~~ (수정 완료): FRACTION check에서 train용 transform 사용

**수정 전** (bg_challenge/generate_in9.py에 아직 남아있음):
```python
# 잘못된 코드 (train이면 256 crop 사용)
crop_size = TEST_CROP if split == "val" else TRAIN_CROP
xform = test_transform if split == "val" else standard_transform
```

**원본 코드 (line 167)**:
```python
post_crop = test_transform(mask)  # 항상 224 crop
```

**수정 후** (in9-dataset-generator/generate_in9.py):
```python
post_crop = test_transform(mask)  # 항상 224 crop (원본과 동일)
```

**영향**: 수정 전에는 train에서 256 crop을 사용하여 FRACTION 기준이 더 관대했음.
224 crop은 이미지 가장자리를 더 많이 잘라내므로 bbox가 차지하는 비율이 더 높아지고,
따라서 더 많은 이미지가 FRACTION>0.9 기준으로 필터링됨.
**결론**: 수정 전에는 원본보다 더 적은 이미지가 필터링 탈락 → 약간 더 많은 이미지 통과.

**왜 원본이 항상 224를 쓰는가**: 최종 평가(test)는 CenterCrop(224)를 사용하므로,
"crop 후 bbox 비율"을 평가 시점 기준으로 체크해야 일관성이 있다.
Train 이미지를 256으로 저장하더라도, 이 필터링은 "평가할 때 쓸모 있는 이미지인가"를 판단하는 것.

---

## 7. Phase 1 (원본 line 226-293 / 우리 phase1_process_superclass line 453-593)

### 처리 흐름 비교

| 단계 | 원본 | 우리 | 동일 |
|------|------|------|:---:|
| 이미지 열거 | `os.listdir(in_ims_dir)` (unsorted) | `sorted([...])` + 확장자 필터 | **차이 #3, #4** |
| 필터링 | `is_good_image(...)` | 동일 로직 | O |
| **GrabCut 1회 시도** | `get_fg_mask(img, ...)` (line 280) | `segmentor.segment(img_np, ...)` (line 518) | O (단일 시도) |
| 실패 시 | `continue` (original/bg_b/bg_t도 저장 안 함) | 동일 | O |
| original 저장 | `img.save(f"{full_dirname}/{synset}_{image_num}.JPEG")` (290) | `img.save(os.path.join(path, out_filename))` (532) | **차이 #5, #6** |
| only_bg_b | `blackout(img, mask)` (274) | 동일 (538) | O |
| only_bg_t | `combine(img, bg_tiled, mask)` (276) | 동일, **but try/except** (544-548) | **차이 #7** |
| **only_fg** | Phase 2에서 생성 (line 357) | Phase 1에서 생성 (line 554) | **차이 #8** |
| **no_fg** | Phase 2에서 생성 (line 358) | Phase 1에서 생성 (line 560) | **차이 #8** |
| **fg_mask.npy** | 저장 안 함 | .npy로 저장 (line 572) | **차이 #8** |

### 차이 #3: 파일 정렬 [임의 판단 — 재현성]

**원본** (`main.py:220`):
```python
im_files = os.listdir(in_ims_dir)
```

**우리** (`generate_in9.py:484-487`):
```python
img_files = sorted([
    f for f in os.listdir(synset_dir)
    if f.lower().endswith((".jpeg", ".jpg", ".png"))
])
```

- **왜**: `os.listdir()`는 OS마다 반환 순서가 다르다 (Linux: inode 순, macOS: 알파벳순 등).
  정렬하면 어떤 OS에서 돌려도 같은 순서로 처리 → 로그 비교/디버깅 용이.
  처리 순서만 다르고 **최종 이미지셋은 동일**.

### 차이 #4: 파일 확장자 필터링 [임의 판단 — defensive coding]

**원본** (`main.py:220,226`):
```python
im_files = os.listdir(in_ims_dir)
for im_file in im_files:
    # 모든 파일을 처리 시도 (ImageNet 디렉토리에 .JPEG만 있다고 가정)
```

**우리** (`generate_in9.py:485-486`):
```python
if f.lower().endswith((".jpeg", ".jpg", ".png"))
```

- **왜**: `.DS_Store`, `Thumbs.db` 같은 비이미지 파일이 섞여있으면 원본은
  `is_good_image()`에서 파일 못 찾아서 에러코드 1 반환 → skip.
  우리는 애초에 목록에 안 넣음. 정상적인 ImageNet에는 `.JPEG`만 있으므로
  **결과 차이 없음**.

### 차이 #5: 파일 이름 형식 [임의 판단 — 코드 스타일]

**원본** (`main.py:235,290`):
```python
image_num = im_file[-10:-5]  # val: "01234" (5자리 추출)
# ...
im.save(f"{full_dirname}/{synset}_{image_num}.JPEG")
# 예: n02279972_01234.JPEG
```

**우리** (`generate_in9.py:529,534`):
```python
out_filename = img_file  # 원본 파일명 그대로
img.save(os.path.join(path, out_filename))
# val 예: ILSVRC2012_val_00001234.JPEG
# train 예: n02279972_1234.JPEG
```

- **왜**: 원본의 `{synset}_{image_num}` 방식은 val 파일명 패턴(`ILSVRC2012_val_000xxxxx.JPEG`)에서
  5자리를 추출하는 로직(`im_file[-10:-5]`)에 의존. train 파일명(`n02279972_1234.JPEG`)에는
  이 파싱이 안 맞음. 원본 파일명을 그대로 쓰면 train/val 모두 문제없이 동작.
  평가 코드(`ImageFolder`)는 파일명을 사용하지 않고 폴더 구조만 보므로 **결과 차이 없음**.
- Mixed 파일명도 형식이 다름:
  - 원본: `fg_n02279972_01234_bg_n02279972_56789.JPEG`
  - 우리: `fg_ILSVRC2012_val_00001234_bg_ILSVRC2012_val_00005678.JPEG` (val) 또는
    `fg_n02279972_1234_bg_n02279972_5678.JPEG` (train)

### 차이 #6: 디렉토리 구조 — synset vs superclass index [의도적 변경 — 기능 확장]
- 원본: `{out_dir}/{variant}/val/{synset}/` (e.g., `output/original/val/n02279972/`)
- 우리: `{out_dir}/{variant}/{split}/{sc_index}/` (e.g., `output/original/train/5/`)
- **이유**: 원본은 단일 synset 처리이므로 synset별 폴더가 자연스러움.
  우리는 9 superclass 전체 처리이고, 평가 코드 (`in9_eval.py`의 `ImageNet9` 클래스)가
  superclass index (0-8) 폴더를 기대하므로 평가와 직접 호환되는 구조 채택.
- **영향**: 평가 시 별도 변환 불필요. 단, 원본 출력과 직접 비교는 불가 (폴더 구조 상이).

### 차이 #7: only_bg_t 실패 시 fallback [임의 판단 — defensive coding]

**원본** (`main.py:275-276`):
```python
bg_tiled = get_bg_tiled(img, xmin, ymin, xmax, ymax)
ims["only_bg_t"] = combine(img, bg_tiled, mask)
# get_bg_tiled 안에서 assert 실패 → ValueError raise → 프로그램 크래시
```

**우리** (`generate_in9.py:546-550`):
```python
try:
    bg_tiled = get_bg_tiled(img, xmin, ymin, xmax, ymax)
    bg_t = combine(img, bg_tiled, bbox_mask)
except ValueError:
    bg_t = bg_b  # tiling 불가 시 blackout과 동일
```

- **왜**: `get_bg_tiled()`는 bbox 외부에 배경 영역이 전혀 없으면 `ValueError`를 raise.
  원본에서 이게 발생하면 프로그램 전체가 크래시. FRACTION check가 bbox > 90%이면 제외하므로
  발생 확률은 극히 낮지만, 45K+ 이미지 처리 중 한 장 때문에 전체가 죽으면 안 됨.
  **영향**: 거의 발생하지 않는 edge case. 만약 발생해도 blackout 이미지가 대신 들어갈 뿐.

### 차이 #8: only_fg/no_fg 생성 시점 + fg_mask 저장 [의도적 변경 — 품질 개선]
- 원본:
  - Phase 1: original, bg_b, bg_t만 저장 (GrabCut 성공해야)
  - Phase 2: GrabCut **재실행** (최대 10회 retry) → only_fg, no_fg, mixed 생성
  - 이유: "Need to redo segmentations from scratch to avoid JPEG compression issues" (line 297-298)
    Phase 1에서 only_fg를 JPEG로 저장 → Phase 2에서 로드하면 pixel==[0,0,0] 체크 시 JPEG 아티팩트로 오류
- 우리:
  - Phase 1: 모든 비합성 variant + fg_mask.npy 저장
  - Phase 2: .npy에서 mask 로드 → only_fg 재생성 → mixed 합성
  - 이유: .npy는 lossless이므로 JPEG 문제 없음. GrabCut 재실행 불필요.
- **영향**: SAM (deterministic)에서는 차이 없음.
  GrabCut (non-deterministic)에서는:
  - 원본: Phase 2에서 다른 mask가 나올 수 있음 (retry로 "더 나은" mask 기대)
  - 우리: Phase 1의 mask를 그대로 사용 (retry 없음)
  - **GrabCut 모드에서 only_fg/no_fg/mixed 결과가 원본과 다를 수 있음**.

---

## 8. Phase 2 (원본 line 299-417 / 우리 phase2_generate_mixed line 640-719)

### 흐름 비교

| 단계 | 원본 | 우리 | 동일 |
|------|------|------|:---:|
| good_images 소스 | `os.listdir(f"{out_dir}/original/val/{synset}")` (300) | `all_good_images[sc_idx]` (메모리 내 메타데이터) | 동일 의미 |
| **GrabCut 재실행 + retry** | 최대 10회 retry (line 340-354) | .npy 로드, retry 없음 (line 669) | **차이 #8 (위 참조)** |
| only_fg 계산 | `img_np * fg_mask[:,:,np.newaxis]` (357) | 동일 (674) | O |
| no_fg 계산 | `img_np * (1-fg_mask[:,:,np.newaxis])` (358) | Phase 1에서 이미 저장 | 동일 결과 |
| FG resize | `standard_transform(only_fg)` (360) | 동일 (675) | O |
| bg_mask 계산 | `np.all(fg_resized == [0,0,0], axis=-1) * 255` (361) | 동일 (681) | O |
| **BG 선택 범위** | 같은 synset (line 303, 365) | 같은 superclass (line 612, 687) | **차이 #9** |
| BG 이미지 로드 | `Image.open(get_image_path(in_dir, bg_synset, ...))` (371) | `Image.open(chosen["img_path"])` (620) | 동일 (원본 이미지 로드) |
| BG annotation 로드 | `ET.fromstring(...)` (375-378) | `chosen["bbox"]` (메타데이터에서) | 동일 의미 |
| BG bbox → bg_tiled | `get_bg_tiled(img, ...)` (393) | 동일 (628) | O |
| BG only_bg_t | `combine(img, bg_tiled, mask)` (394) | 동일 (629) | O |
| BG resize | `standard_transform(only_bg_t)` (395) | 동일 (699) | O |
| 합성 | `combine(fg_resized, bg_resized, bg_mask)` (397) | 동일 (700) | O |
| **mixed 종류** | `mixed_same` 만 (line 303: `bgclass_synsets["same"] = synset`) | same, rand, next (line 686-689) | **차이 #10** |
| **mixed_rand BG 선택** | 미구현 | `random.choice(other_scs)` (688) | **차이 #10** |
| **mixed_next BG 선택** | 미구현 | `(superclass_idx + 1) % 9` (689) | **차이 #10** |

### 차이 #9: BG 선택 범위 — synset vs superclass [의도적 변경 — 논문 정의 준수]
- 원본: `get_rand_image(f"{out_dir}/original/val/{synset}")` → 같은 **synset** 내에서 BG 선택
  - 이유: 원본 코드가 단일 synset 단위로 동작하는 구조적 한계 (다른 synset 데이터 없음)
  - 논문 Section 3: "same class"라고 기술 → IN-9의 "class"는 **superclass**
- 우리: `all_good_images[bg_sc]` → 같은 **superclass** 전체 synset에서 BG 선택
  - 예: SC 0 (Dog, 116 synsets) → 116개 synset 모두의 good_images 풀에서 선택
- **영향**:
  - 원본 (synset 범위): mixed_same BG가 FG와 매우 유사한 종 (e.g., 같은 개 품종)
  - 우리 (superclass 범위): mixed_same BG가 같은 대분류지만 다른 종 (e.g., 다른 개 품종)
  - BG-Gap 측정에 영향 가능: BG가 FG와 더 유사할수록 "hint" 효과 강화 → BG-Gap 증가
  - 그러나 **논문 정의상 superclass 단위가 맞음** (원본 코드의 synset 제한은 구현의 한계)

### 차이 #10: mixed_rand, mixed_next 구현 [의도적 변경 — 기능 확장]
- 원본: 미구현 (주석에 "Change the background class to make mixed_rand and mixed_next" 언급)
- 우리: 구현
  - mixed_rand: 현재 SC를 제외한 8개 SC 중 랜덤 1개에서 BG 선택
  - mixed_next: `(SC + 1) % 9` 에서 BG 선택
- **영향**: 신규 기능이므로 원본과 비교 대상 아님. BG-Gap 측정에 필수.

---

## 9. only_fg, no_fg 저장 (원본 line 356-358, 402-406)

| 항목 | 원본 | 우리 | 동일 |
|------|------|------|:---:|
| only_fg 계산 | `img_np * fg_mask[:,:,np.newaxis]` | 동일 | O |
| no_fg 계산 | `img_np * (1-fg_mask[:,:,np.newaxis])` | 동일 | O |
| 저장 시점 | Phase 2 (GrabCut 재실행 후) | Phase 1 (첫 세그먼트 직후) | **차이 #8** |
| 저장 형식 | `.JPEG` | `.JPEG` (only_fg, no_fg) + `.npy` (fg_mask) | 동일 + 추가 |

---

## 10. get_rand_image() → _pick_bg_image() (원본 line 190-197 / 우리 line 600-637)

| 항목 | 원본 | 우리 | 동일 |
|------|------|------|:---:|
| 후보 목록 | `os.listdir(bg_dir)` (191) | `all_good_images[bg_sc]` (612) | 동일 의미 |
| 선택 방법 | `np.random.randint(num_imgs)` (193) | `random.choice(candidates)` (618) | 동일 의미 |
| 파일명 파싱 | `img_file.strip(".JPEG").split("_")` (196) | 불필요 (메타데이터에 이미 정보 있음) | - |
| **실패 시 retry** | 없음 (단일 시도) | 최대 10회 retry (line 617) | **차이 #11** |

### 차이 #11: BG 이미지 선택 시 retry [임의 판단 — defensive coding]

**원본** (`main.py:190-197`):
```python
def get_rand_image(img_dir):
    all_imgs = os.listdir(img_dir)
    num_imgs = len(all_imgs)
    img_num = np.random.randint(num_imgs)
    img_file = all_imgs[img_num]
    synset, image_num = img_file.strip(".JPEG").split("_")
    return synset, image_num
# 1회 호출, 실패 처리 없음
```

**우리** (`generate_in9.py:618-637`):
```python
max_attempts = 10
for _ in range(max_attempts):
    chosen = random.choice(candidates)
    try:
        img = Image.open(chosen["img_path"]).convert("RGB")
        # ... BG tiling + combine ...
        return only_bg_t, chosen["synset"], bg_stem
    except Exception:
        continue
return None, None, None
```

- **왜**: 원본은 BG 이미지가 이미 `original/`에 저장된 상태라 로드 실패 가능성을 고려 안 함.
  우리는 메타데이터의 `img_path`(원본 ImageNet 경로)에서 다시 로드하므로 파일 접근 실패가
  이론적으로 가능. 10회 retry로 안전하게 처리. 정상 상황에서는 1회 만에 성공하므로
  **결과 차이 없음**.

---

## 11. main() 구조 (원본 line 201-421 / 우리 line 786-977)

| 항목 | 원본 | 우리 | 차이 |
|------|------|------|------|
| argparse | `--in_dir, --out_dir, --ann_dir, --synset` | 동일 + 추가 옵션 | **#12** |
| 경로 구성 | `f"{args.in_dir}/val"` 하드코딩 | `--split train/val` 지원 | **#12** |
| synset 처리 | 단일 `--synset` | 9 superclass × 370 synset 자동 | **#12** |
| **random seed** | 없음 | `random.seed(42)`, `np.random.seed(42)` | **#13** |
| Phase 1 → Phase 2 | 연속 실행 (같은 synset) | Phase 1 전체 완료 후 Phase 2 | **#14** |
| **IN-9L** | 없음 | `generate_in9l()` 구현 | **#15** |

### 차이 #12: 다중 synset + train/val 지원 [의도적 변경 — 기능 확장]
- 원본: 단일 synset, val only
- 우리: 9 superclass (370 synset), train/val 모두 지원
- **영향**: 기능 확장이므로 원본의 val 단일 synset 동작은 동일하게 재현 가능.

### 차이 #13: random seed 설정 [임의 판단 — 재현성]

**원본** (`main.py`): seed 설정 없음
```python
# get_rand_image에서 np.random.randint(num_imgs)로 BG 선택 — 매 실행 다른 결과
```

**우리** (`generate_in9.py:821-822`):
```python
random.seed(args.seed)    # default 42
np.random.seed(args.seed)
```

- **왜**: 논문용 코드이므로 재현성이 중요. 같은 seed로 같은 결과를 얻을 수 있어야
  리뷰어가 검증 가능. 원본은 val 이미지 수십 장 수준이라 재현성이 덜 중요했고,
  우리는 45K+ 이미지에서 mixed variant의 BG 선택이 랜덤이므로 seed 고정이 필수적.
  **영향**: mixed variant의 구체적인 BG 이미지 선택이 달라지지만, 충분히 많은 이미지에서
  랜덤 선택하므로 통계적으로 유의미한 차이 없음.

### 차이 #14: Phase 구조 [의도적 변경 — 기능 확장]
- 원본: Phase 1 (한 synset) → Phase 2 (한 synset) → 끝
- 우리: Phase 1 (전체 9 SC) → Phase 2 (전체 9 SC)
- **이유**: mixed_rand/mixed_next에서 다른 superclass의 good_images가 필요하므로
  Phase 1이 모든 SC에 대해 완료되어야 Phase 2 실행 가능.
- **영향**: mixed_same만 생성할 경우 결과 동일. mixed_rand/next는 이 구조 필수.

### 차이 #15: IN-9L 구현 [의도적 변경 — 기능 확장]
- 원본: 미구현
- 우리: `generate_in9l()` — 세그먼트 없이 balanced sampling, 20K/class
- **영향**: 신규 기능.

---

## 12. 기타 차이

### 차이 #16: 이미지/annotation 로드 에러 처리 [임의 판단 — defensive coding]

**원본** (`main.py:251,256-258`):
```python
img = Image.open(image_path).convert("RGB")
# ...
annotation_file = open(annotation_path)
root = ET.fromstring(annotation_file.read())
# 에러 시 예외 발생 → 프로그램 크래시
```

**우리** (`generate_in9.py:498-508`):
```python
try:
    img = Image.open(img_path).convert("RGB")
except Exception:
    stats["load_error"] += 1
    continue

try:
    ann_w, ann_h, bboxes = parse_annotation(ann_path)
except Exception:
    stats["ann_parse_error"] += 1
    continue
```

- **왜**: 원본은 val set 50개 이미지를 수동으로 다루는 스크립트라 에러 처리 없이도 괜찮음.
  우리는 9 superclass × 370 synset × 수천 이미지를 자동 처리하므로, 손상된 JPEG 하나
  때문에 전체가 죽으면 안 됨. skip + 통계 기록으로 처리.
  정상 ImageNet에서는 **결과 차이 없음**.

### 차이 #17: annotation 경로 탐색 [의도적 변경 — 기능 확장]
- 원본: `{ann_dir}/ILSVRC2012_val_000{image_num}.xml` 고정 (val 패턴)
- 우리: 여러 후보 경로 탐색 (line 376-398)
  - Train: `{ann_dir}/{synset}/{stem}.xml` 또는 `{ann_dir}/train/{synset}/{stem}.xml`
  - Val: `{ann_dir}/val/{stem}.xml` 또는 `{ann_dir}/{stem}.xml`
- **이유**: train annotation은 synset별 하위 디렉토리에 있음.
- **영향**: val 모드에서 원본과 동일한 경로 탐색. train 지원 추가.

### 차이 #18: make_if_not_exists [임의 판단 — 코드 스타일]

**원본** (`main.py:20-24`):
```python
def make_if_not_exists(dirname):
    if os.path.exists(dirname):
        pass
    else:
        os.makedirs(dirname)
```

**우리** (`generate_in9.py:266`):
```python
def make_if_not_exists(dirname):
    os.makedirs(dirname, exist_ok=True)
```

- **왜**: Python 3.2+에서 `exist_ok=True`가 추가되어 한 줄로 동일한 동작 구현 가능.
  원본은 `exists()` 체크와 `makedirs()` 사이에 race condition 가능성도 있음 (다른 프로세스가
  같은 폴더를 동시에 만들 때). **동작 완전 동일**.

---

## 13. 완전한 차이점 총괄표

| # | 차이점 | 분류 | 결과 영향 | 비고 |
|---|--------|------|----------|------|
| 1 | `except:` → `except Exception:` | 임의/스타일 | 없음 | |
| 2 | bbox_area==0 체크 추가 | 임의/defensive | 없음 | ImageNet에 해당 케이스 없음 |
| 3 | 파일 정렬 (`sorted()`) | 임의/재현성 | 없음 | 처리 순서만 |
| 4 | 파일 확장자 필터링 | 임의/defensive | 없음 | .DS_Store 등 방지 |
| 5 | 파일명 형식 | 임의/스타일 | 없음 | 평가 코드는 파일명 무시 |
| 6 | 디렉토리 구조 (SC index) | 의도적/기능 | 없음 | 평가 코드와 호환 |
| 7 | only_bg_t fallback | 임의/defensive | 극히 드묾 | FRACTION 필터가 먼저 걸러냄 |
| 8 | only_fg/no_fg 시점 + .npy | 의도적/품질 | GrabCut만 | SAM은 차이 없음 |
| 9 | BG 선택 범위 (SC vs synset) | 의도적/논문 | **있음** | 논문 정의 = SC 단위 |
| 10 | mixed_rand/next 구현 | 의도적/기능 | 신규 | 원본 미구현 |
| 11 | BG 선택 retry | 임의/defensive | 없음 | edge case |
| 12 | 다중 synset + train 지원 | 의도적/기능 | 구조적 | 원본은 val only |
| 13 | random seed | 임의/재현성 | 미미 | 통계적 무차이 |
| 14 | Phase 구조 변경 | 의도적/기능 | 없음 | mixed_rand/next 필수 |
| 15 | IN-9L 구현 | 의도적/기능 | 신규 | 원본 미구현 |
| 16 | 이미지/annotation 에러 처리 | 임의/defensive | 없음 | 정상 파일에서 |
| 17 | annotation 경로 탐색 | 의도적/기능 | 없음 | train 지원 |
| 18 | make_if_not_exists | 임의/스타일 | 없음 | 동일 동작 |

---

## 14. 결론

### 결과에 실질적 영향을 주는 차이점: **2가지**만

1. **#9 — BG 선택 범위 (synset vs superclass)**
   - 원본: 같은 synset (구현 한계)
   - 우리: 같은 superclass (논문 정의)
   - 이것은 의도적인 변경이며, 논문의 "same class" 정의를 따른 것.
   - BG-Gap에 약간의 영향 가능하나, 논문 범위(6-22%) 내일 것으로 예상.

2. **#8 — GrabCut retry 없음 (GrabCut 모드 전용)**
   - SAM 3 모드에서는 영향 없음 (deterministic).
   - GrabCut 모드에서는: Phase 2의 mask가 Phase 1과 동일 (retry 없음).
   - 원본은 Phase 2에서 재실행 + retry → 다른 mask 가능.
   - **우리의 주 사용 segmentor는 SAM 3이므로 실질적 영향 없음**.

### 나머지 16개 차이점
- 임의 판단 (defensive coding, 재현성, 스타일): **결과 영향 없음**
- 의도적 변경 (기능 확장): **원본에 없는 신규 기능**

### "원본 재현"을 위한 체크리스트

GrabCut 모드 (`--segmentor grabcut`)로 val set을 단일 synset 처리하면,
차이 #9 (BG 범위)를 제외하고 원본과 동일한 결과를 재현할 수 있다.
BG 범위 차이도 단일 synset 모드에서는 해당 synset만 있으므로 사실상 동일.
